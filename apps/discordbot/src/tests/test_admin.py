# def test_leave(client):
#     url = client.app.url_for("admin-leave")
#     response = client.get(url)
#     assert response.status_code == 200

# def test_chat(client):
#     url = client.app.url_for("admin-chat")
#     response = client.get(url)
#     assert response.status_code == 200

# def test_guilds(client):
#     url = client.app.url_for("admin-guilds")
#     response = client.get(url)
#     assert response.status_code == 200

# def test_channels(client):
#     url = client.app.url_for("admin-channels")
#     response = client.get(url)
#     assert response.status_code == 200
